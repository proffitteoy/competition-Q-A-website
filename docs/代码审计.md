请审计并重构这段代码，默认系统中不存在天然可信输入。

审计重点：
1. 找出所有被隐式信任的数据来源：
   - 前端请求参数
   - Cookie / Header / JWT 中未充分验证的字段
   - 内部 API 返回值
   - 数据库现存字段
   - 缓存内容
2. 找出所有“来源即可信”的错误假设。
3. 找出越权、参数篡改、伪造身份、水平越权、垂直越权、注入、状态污染风险。
4. 将代码改成：
   - 显式输入校验
   - 服务端鉴权
   - 最小权限
   - 防御性解析
   - fail-closed
5. 不要只给建议，要直接给出修改后的代码。
6. 对每一处关键修改，注明它修复了什么信任问题。