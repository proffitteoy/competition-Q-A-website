# 上线差距评估（2026-05-01，二轮审计后）

## 总结
当前仓库已从“可运行 MVP 后端”进入“上线差距收口”阶段。  
本轮已补齐权限回归、上传驱动抽象、通知富文本与草稿能力、仪表盘细化指标，以及迁移/回滚与回归清单文档。  
二轮审计新增修复：登录仅允许 `active` 账号、邮箱统一小写、上传接口强制登录并绑定 `competitionId`、新增调试管理员创建脚本。

## 必须完成（上线前）

| 优先级 | 模块 | 当前状态 | 预计工作量 |
| --- | --- | --- | --- |
| P0 | 生产数据库与迁移流程固化 | 已新增《生产数据库迁移与回滚手册》 | 已完成 |
| P0 | 认证安全加固 | 已补 `AUTH_SECRET` 生产校验、会话策略与 fallback 收紧 | 已完成 |
| P0 | 后台关键 API 权限回归 | 已补作用域 fail-closed + `test:permissions` 回归用例 | 已完成 |
| P0 | 上传链路生产化 | 已支持 `UPLOAD_STORAGE_DRIVER=local/s3` 与 `S3_*` 配置 | 已完成（待环境联调） |
| P0 | 端到端回归 | 已新增《端到端回归执行清单》模板 | 待执行 |
| P0 | 调试与应急账号保障 | 已提供 `npm run admin:create-debug` 脚本 | 待在可联网环境执行一次 |

## 建议完成（可上线后第一迭代）

| 优先级 | 模块 | 当前状态 | 预计工作量 |
| --- | --- | --- | --- |
| P1 | 通知富文本编辑器 | 已接入富文本编辑与真实 API CRUD | 已完成 |
| P1 | 仪表盘指标细化 | 已新增待审核数、本周新增、通过率、状态分布图 | 已完成 |
| P1 | 草稿暂存 | 报名表单已支持本地草稿暂存/恢复 | 已完成 |

## 后续迭代（非上线阻塞）

| 优先级 | 模块 | 当前状态 | 预计工作量 |
| --- | --- | --- | --- |
| P2 | Q&A 问答系统 | 仍为占位 | 5-7 天 |

## 本次评估依据
- 已完成：登录/注册接通、`/admin/*` 路由守卫、比赛管理 CRUD、用户角色管理、报名审核、上传与报名提交闭环、通知富文本编辑、草稿暂存、作用域越权收紧。
- 二轮审计修复项：
  - `Auth` 登录拒绝 `disabled/pending_verification` 账号。
  - 注册与登录统一邮箱小写，降低大小写重复账号风险。
  - 上传接口要求登录且必须带 `competitionId`，收敛匿名上传面。
- 待确认：生产 S3/OSS 凭证联调、上线前完整 E2E 清单执行记录、调试管理员脚本在目标库执行结果留档。
