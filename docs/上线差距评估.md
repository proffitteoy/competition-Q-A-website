# 上线差距评估（2026-05-01，下一工作阶段：联调与预发布）

## 阶段结论
- 截至 2026-05-01，项目从”二轮审计修复”切换到”联调与预发布”阶段。
- P1 功能项（通知富文本、仪表盘细化、草稿暂存、调试样例文档）已基本收口，不再作为上线阻塞。
- 当前上线主阻塞集中在 P0：数据库环境配置与联调验证、上传存储联调、端到端回归留档。

## 已完成沉淀（作为本阶段输入）
- 认证与权限：登录仅允许 `active`、邮箱统一小写、后台作用域 `fail-closed`、`test:permissions` 回归用例已补齐。
- 生产化基础：上传驱动支持 `local/s3`，并已补《生产数据库迁移与回滚手册》《端到端回归执行清单》。
- 调试保障：已提供 `npm run admin:create-debug`；已补《调试样例与管理员入口》并覆盖比赛/通知/报名多状态样例。

## 本轮修复（2026-05-01 安全与体验加固）

### 安全修复
| 模块 | 修复内容 | 状态 |
| --- | --- | --- |
| 环境变量会话回退 | 生产环境硬性禁止 env_fallback，无论环境变量如何配置 | 已完成 |
| 开放重定向 | 登录/注册 callbackUrl 增加同源校验，阻止外部域跳转 | 已完成 |
| 注册接口信息泄露 | 统一邮箱/学号冲突提示，catch 块不再暴露内部错误 | 已完成 |
| 密码策略 | 最低 8 位 + 必须含字母和数字，bcrypt cost 提升至 12 | 已完成 |
| 上传错误码 | 区分客户端错误(400)与服务端错误(500)，不再一律返回 400 | 已完成 |

### 上传链路修复
| 模块 | 修复内容 | 状态 |
| --- | --- | --- |
| S3 文件读取 | GET 端点支持 S3 存储驱动（新增 readS3File + readUploadedFile 调度） | 已完成 |
| 本地存储警告 | 生产环境未配置 UPLOAD_LOCAL_DIR 时输出 console.warn | 已完成 |

### 管理端体验修复
| 模块 | 修复内容 | 状态 |
| --- | --- | --- |
| 仪表盘统计卡片 | 加载中显示 “--” 而非误导性的 “0” | 已完成 |
| 审核对话框 | 成功后自动关闭；所有按钮显示加载状态；textarea 增加 aria-label | 已完成 |
| 赛程页面 | 增加空态提示、加载骨架屏、错误兜底、空时间线提示 | 已完成 |

### 代码质量
| 模块 | 修复内容 | 状态 |
| --- | --- | --- |
| 类型解耦 | 新建 src/lib/types.ts，auth/permissions/services/admin 页面不再从 mock-data 导入类型 | 已完成 |

## P0（上线阻塞，当前阶段必须完成）

| 优先级 | 模块 | 当前状态 | 下一动作 | 目标时间 |
| --- | --- | --- | --- | --- |
| P0 | 调试管理员账号落库验收 | 脚本已具备，尚未在目标库留存执行结果 | 执行 `npm run admin:create-debug`，记录账号、执行时间、执行人 | 2026-05-02 |
| P0 | 上传链路生产联调 | 代码已支持 S3 读写，未完成真实凭证联调 | 完成 1 次真实上传、1 次超限拦截、1 次鉴权失败验证 | 2026-05-03 |
| P0 | 端到端回归 | 已有清单模板，未形成执行记录 | 按《端到端回归执行清单》跑完关键链路并关闭阻塞缺陷 | 2026-05-03 |

## P1（上线后首迭代，可并行准备）

| 优先级 | 模块 | 当前状态 | 下一动作 | 目标时间 |
| --- | --- | --- | --- | --- |
| P1 | 文档与运维交接 | 已有分散文档 | 汇总上线操作单页（环境变量、迁移、seed、回滚、管理员入口） | 2026-05-05 |
| P1 | 回归自动化增强 | 当前以手工清单为主 | 将关键回归脚本接入 CI（至少权限回归 + 基础接口健康检查） | 2026-05-06 |
| P1 | 管理端体验收尾 | 本轮已修复核心空态/加载/错误问题 | 剩余：关键词筛选防抖、native confirm 替换为 Dialog、aria 标签补全 | 2026-05-06 |
| P1 | 登录安全增强 | 无速率限制、JWT 角色不刷新 | 接入速率限制中间件；考虑 JWT 短期过期 + session 刷新策略 | 2026-05-07 |

## P2（后续迭代，非本阶段阻塞）

| 优先级 | 模块 | 当前状态 | 预计工作量 |
| --- | --- | --- | --- |
| P2 | Q&A 问答系统 | 初步实现已在 #010/#011 落地 | 5-7 天（完善） |
| P2 | 忘记密码流程 | 当前为占位 stub | 2-3 天 |
| P2 | 邮箱验证 | 注册后直接 active，无验证 | 2-3 天 |

## 阶段退出标准（进入”可上线候选”前必须满足）
1. 目标环境 `db:migrate`、`seed:debug`、`admin:create-debug` 执行成功并有记录。
2. 上传链路在目标存储完成通过/拒绝用例验证。
3. 端到端回归清单执行完成，P0 缺陷清零。
4. 回滚演练完成且结果可复现。
5. 上线操作文档与责任人清单完成确认。
